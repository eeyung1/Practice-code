Itoa

Function Itoa(n):
    If n is 0:
        Return "0"
    
    Initialize result as an empty string
    Initialize isNegative as false
    
    If n is less than 0:
        Set isNegative to true
        Set n to -n (work with the absolute value of n)
    
    While n is greater than 0:
        Set digit to n mod 10  // Get the last digit of n
        Convert digit to a character by adding '0' to digit
        Prepend the character to the result string // result = string('0' + digit) + result
        Set n to n divided by 10  // Remove the last digit from n
    
    If isNegative is true:
        Prepend "-" to the result string
    
    Return result

ThirdTimeIsACharm

Function ThirdTimeIsACharm(str):
    Initialize an empty string result

    If the length of str is 0:
        Return newline (i.e., "\n")

    For i = 2 to length of str - 1 with step size 3:
        Append str[i] to result
    
    Return result followed by a newline ("\n")


    EXPANDSTR

    Function Main():
    If the number of arguments is not 1:
        Return (Do nothing)

    Get the string argument (str)

    Trim leading and trailing spaces from str

    Split str into words using spaces as the delimiter:
        words = Split(str by spaces)

    If words is empty:
        Return (Do nothing)

    Join the words with exactly three spaces between each word:
        result = Join(words with "   ")

    Print result followed by a newline ("\n")


    FromTo

    Function FromTo(from, to):
    If from < 0 or from > 99 or to < 0 or to > 99:
        Return "Invalid\n"

    Initialize an empty string result

    If from <= to:
        For i = from to to (inclusive):
            Format i as a two-digit string (with leading zero if necessary)
            Append formatted string to result
            If i is not the last number in the range:
                Append ", " to result
    Else:
        For i = from down to to (inclusive):
            Format i as a two-digit string (with leading zero if necessary)
            Append formatted string to result
            If i is not the last number in the range:
                Append ", " to result

    Append "\n" to the result

    Return result


    printrevcomb

    Function PrintRevComb():
    Initialize an empty list "combinations"

    // Loop through all possible combinations of three digits
    For i = 9 down to 2:
        For j = i-1 down to 1:
            For k = j-1 down to 0:
                // Check if digits are distinct and in strictly descending order (i > j > k)
                If i > j > k:
                    // Format the combination as a string "i, j, k" and append it to the list
                    Append formatted string "i, j, k" to combinations

    // Join all combinations with a comma and space
    result = Join combinations with ", "

    // Print the final result followed by a newline
    Print result + "\n"



